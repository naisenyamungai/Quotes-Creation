import { HighestupvoteDirective } from './highestupvote.directive';

describe('HighestupvoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighestupvoteDirective();
    expect(directive).toBeTruthy();
  });
});
